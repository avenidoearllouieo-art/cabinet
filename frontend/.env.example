# The Portal API uses the Vite proxy during local development.
VITE_API_BASE_URL=/api

# The phone must open the Portal through a network-accessible address.
# This value is normally used by the Cabinet QR configuration instead.
# VITE_WEB_APP_BASE_URL=http://192.168.1.100:5176